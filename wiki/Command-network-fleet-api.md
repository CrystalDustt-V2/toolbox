# toolbox network fleet-api

Start the Local API Gateway for remote fleet management.

Source: [src/toolbox/plugins/network/__init__.py:L580](https://github.com/CrystalDustt-V2/toolbox/blob/master/src/toolbox/plugins/network/__init__.py#L580)

## Synopsis

```bash
toolbox network fleet-api [-p INTEGER] [--host TEXT]
```

## Examples

```bash
toolbox network fleet-api -p <port> --host <host>
```

## Help

```text
Usage: toolbox network fleet-api 
           [OPTIONS]

  Start the Local API Gateway for remote fleet
  management.

Options:
  -p, --port INTEGER  Port for the Fleet API
  --host TEXT         Host to bind to
  --help              Show this message and exit.
```

Parent: [toolbox network](Command-network)
Back: [Command Index](Command-Index)
