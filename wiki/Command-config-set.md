# toolbox config set

Update a setting (key value). Use dots for nested keys (e.g. engine_paths.ffmpeg).

Source: [src/toolbox/cli.py:L75](https://github.com/CrystalDustt-V2/toolbox/blob/master/src/toolbox/cli.py#L75)

## Synopsis

```bash
toolbox config set KEY VALUE
```

## Examples

```bash
toolbox config set <key> <value>
```

## Help

```text
Usage: toolbox config set [OPTIONS] KEY VALUE

  Update a setting (key value). Use dots for nested
  keys (e.g. engine_paths.ffmpeg).

Options:
  --help  Show this message and exit.
```

Parent: [toolbox config](Command-config)
Back: [Command Index](Command-Index)
