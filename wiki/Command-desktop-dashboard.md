# toolbox desktop dashboard

Start the local ToolBox dashboard.

Source: [src/toolbox/plugins/desktop/__init__.py:L26](https://github.com/CrystalDustt-V2/toolbox/blob/master/src/toolbox/plugins/desktop/__init__.py#L26)

## Synopsis

```bash
toolbox desktop dashboard [--host TEXT] [--port INTEGER]
```

## Examples

```bash
toolbox desktop dashboard --host <host> --port <port>
```

## Help

```text
Usage: toolbox desktop dashboard 
           [OPTIONS]

  Start the local ToolBox dashboard.

Options:
  --host TEXT     Host to bind to
  --port INTEGER  Port to bind to
  --help          Show this message and exit.
```

Parent: [toolbox desktop](Command-desktop)
Back: [Command Index](Command-Index)
